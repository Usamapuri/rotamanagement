{
  "name": "dreamshift",
  "version": "1.0.0",
  "description": "Rota Management System",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node scripts/seedDB.js && node server.js",
    "dev": "concurrently \"nodemon server.js\" \"cd client && npm start\"",
    "seed": "node scripts/seedDB.js",
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "heroku-postbuild": "npm run install-client && npm run build-client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pjhellstrom/dreamshift.git"
  },
  "keywords": ["rota", "management", "shifts", "scheduling"],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pjhellstrom/dreamshift/issues"
  },
  "homepage": "https://github.com/pjhellstrom/dreamshift#readme",
  "dependencies": {
    "axios": "^0.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-session": "^1.16.2",
    "mongoose": "^5.7.0",
    "passport": "^0.4.0",
    "passport-local": "^1.0.0",
    "passport-local-mongoose": "^5.0.1"
  },
  "devDependencies": {
    "nodemon": "^1.19.2",
    "concurrently": "^4.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
